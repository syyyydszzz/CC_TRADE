# Example Strategies

These directories are fixtures and examples only.

- They are not the workflow authority for this template repo.
- New real strategy work should happen in a generated single-strategy repo.
- Use `bash scripts/new-strategy-repo.sh ...` from the template repo root to create that new repo.
